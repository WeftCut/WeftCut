{
  "tools": [
    {
      "name": "ping",
      "description": "Liveness check. Returns 'pong' to confirm the WeftCut MCP server is reachable.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Empty arg shape for tools that take no parameters. The dispatch table\ndeserializes `{}` (or any object) into this; `schemars` advertises it as an\nempty object schema.",
        "title": "EmptyArgs",
        "type": "object"
      }
    },
    {
      "name": "apply_subtitles",
      "description": "Import a subtitle document (SRT/VTT/ASS) as a caption track of editable Text layers. Cue timings come from the body. `format` is sniffed when omitted. Advanced ASS styling (karaoke, drawings) is simplified. Returns the new caption track id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "body": {
            "description": "Subtitle document body (SRT, ASS, or VTT).",
            "type": "string"
          },
          "format": {
            "description": "'srt', 'ass', or 'vtt'. Sniffed from body when omitted.",
            "type": [
              "string",
              "null"
            ]
          },
          "t_end_us": {
            "description": "IGNORED — cue timings come from the body, not the timeline envelope.\nKept for wire-schema stability; do not remove.",
            "format": "int64",
            "type": "integer"
          },
          "t_start_us": {
            "description": "IGNORED — cue timings come from the body, not the timeline envelope.\nKept for wire-schema stability; do not remove.",
            "format": "int64",
            "type": [
              "integer",
              "null"
            ]
          },
          "track_id": {
            "description": "IGNORED — a caption import always creates its own Caption-role track.\nKept for wire-schema stability; do not remove.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "body",
          "t_end_us"
        ],
        "title": "ApplySubtitlesArgs",
        "type": "object"
      }
    },
    {
      "name": "detect_silences",
      "description": "Find silent regions in a VideoClip or Audio layer using the pre-computed waveform. Walks the layer's VPEAKS file using its exact PCM timebase and returns timeline-absolute ranges where every peak stays below `threshold_amp` for at least `min_silence_us` microseconds. Defaults: `threshold_amp=0.02` (-34 dBFS), `min_silence_us=500000` (0.5s). Use the returned ranges to feed `split_layer` + `delete_layer` and produce a tighter cut. Returns `[{ t_start_us, t_end_us }, ...]` sorted by t_start_us. Errors with `NotReady` if the waveform job hasn't finished yet — wait for a `media:job_complete` event with `kind=waveform` and retry.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "layer_id": {
            "description": "Target VideoClip or Audio layer id.",
            "type": "string"
          },
          "min_silence_us": {
            "description": "Minimum contiguous silence duration (microseconds) to surface.\nDefault 500000 (0.5 seconds).",
            "format": "int64",
            "type": [
              "integer",
              "null"
            ]
          },
          "threshold_amp": {
            "description": "Peak amplitude threshold in [0.0, 1.0]. Anything strictly below this\ncounts as silence. Default 0.02 (≈ -34 dBFS).",
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          }
        },
        "required": [
          "layer_id"
        ],
        "title": "DetectSilencesArgs",
        "type": "object"
      }
    },
    {
      "name": "analyze_clip",
      "description": "Detect shot boundaries in a VideoClip layer and return the shot list plus per-shot pixel stats. Runs a deterministic detector over the layer's source (preferring the 720p proxy) and returns `{ shots: [{ index, t_start_us, t_end_us, keyframe_t_us, brightness, motion, sharpness, flags: [ ... ] }], cut_scores: [{ t_us, score }] }`. All timestamps are SOURCE-ABSOLUTE microseconds, clipped to the layer's source window. `cut_scores` is the raw cut signal (one entry per detected cut, `score` in 0..1); `shots` is the cleaned segmentation (cuts closer than `min_shot_us` merged). Per shot: `keyframe_t_us` is a representative cover-frame time (the midpoint); `brightness` is mean luma (0..1); `sharpness` is a focus proxy (variance of the Laplacian, higher = sharper); `motion` is how much the shot's endpoints differ (0..1); `flags` may include `\"black\"`, `\"freeze\"`, `\"fade\"`. Use the shots to split, trim, drop bad takes, or pick a cover frame. Optional `sensitivity` (0..1 cut threshold, default 0.4; lower = more cuts), `min_shot_us` (minimum shot duration, default 500000), and `passes` (subset of `[\"shots\", \"stats\", \"events\"]`, default all — drop `\"stats\"` / `\"events\"` to skip the per-shot frame sampling and return timing only). VideoClip layers only; any other layer kind errors.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "layer_id": {
            "description": "Target VideoClip layer id.",
            "type": "string"
          },
          "min_shot_us": {
            "description": "Minimum shot duration (microseconds); cuts closer than this are merged.\nDefault 500000 (0.5 seconds).",
            "format": "int64",
            "type": [
              "integer",
              "null"
            ]
          },
          "passes": {
            "description": "Which passes to run — a subset of `[\"shots\", \"stats\", \"events\"]`.\nDefault all. Drop `\"stats\"` / `\"events\"` to skip per-shot frame sampling\nand return timing only.",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "sensitivity": {
            "description": "Cut sensitivity in [0.0, 1.0]: a frame whose shot-change score exceeds\nthis starts a new shot. Lower = more (finer) cuts. Default 0.4.",
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          }
        },
        "required": [
          "layer_id"
        ],
        "title": "AnalyzeClipArgs",
        "type": "object"
      }
    },
    {
      "name": "compare_frames",
      "description": "Compare two video frames for perceptual similarity — dedup shots or match a cutaway. Args `{ a: { layer_id, t_us }, b: { layer_id, t_us } }`; each side names a VideoClip layer and a SOURCE-ABSOLUTE timestamp (microseconds) in the same coordinate space as `media://{id}/frame/<t_us>` and `analyze_clip`'s `keyframe_t_us`, so a shot cover frame drops straight in. The two sides may point at the same clip or different clips. Samples one frame per side and returns `{ phash_hamming, ssim, similar }`: `phash_hamming` is the 0..64 Hamming distance between the frames' DCT perceptual hashes (0 = identical, small = the same frame re-encoded / rescaled); `ssim` is MSSIM structural similarity in 0..1 (1.0 = identical); `similar` is true when `phash_hamming <= 10 && ssim >= 0.5` — both the hash and the structural score must agree. The pHash is the strong signal (0 for the same frame re-encoded, 20+ for a different scene); the loose SSIM floor keeps a source frame vs its lossy downscaled proxy similar while still rejecting unrelated frames. Read-only; VideoClip layers only (any other layer kind, or a missing/non-video source, errors naming the offending side).",
      "inputSchema": {
        "$defs": {
          "FrameRef": {
            "description": "One side of a `compare_frames` pair. `#[schemars(skip)]` on the injected\n`layer` / `media` keeps each side's ADVERTISED schema exactly\n`{ layer_id, t_us }`; serde still deserializes the host-injected slice.",
            "properties": {
              "layer_id": {
                "description": "Target VideoClip layer id.",
                "type": "string"
              },
              "t_us": {
                "description": "SOURCE-ABSOLUTE timestamp (microseconds) of the frame to sample — the\nsame coordinate space as `media://{id}/frame/<t_us>` and the\n`keyframe_t_us` / `t_*_us` that `analyze_clip` returns, so a shot cover\nframe can be fed straight in.",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "layer_id",
              "t_us"
            ],
            "type": "object"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "a": {
            "$ref": "#/$defs/FrameRef",
            "description": "First frame to compare."
          },
          "b": {
            "$ref": "#/$defs/FrameRef",
            "description": "Second frame to compare."
          }
        },
        "required": [
          "a",
          "b"
        ],
        "title": "CompareFramesArgs",
        "type": "object"
      }
    },
    {
      "name": "import_media",
      "description": "Import a media file from an absolute path. Hashes the file (blake3) and probes metadata via ffprobe when installed. Returns the new media id.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "path": {
            "description": "Absolute path to a video / audio / image / subtitle file the host can read.",
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "title": "ImportMediaArgs",
        "type": "object"
      }
    },
    {
      "name": "transcribe_clip",
      "description": "Transcribe a VideoClip or Audio layer through the configured transcription provider (cloud OpenAI Whisper, or local whisper.cpp / FunASR) and return a normalized transcript as JSON: `{ backend, segments: [{ t_start_us, t_end_us, text, words: [{ t_start_us, t_end_us, text }] }], language, word_timing, srt }`. All timestamps are timeline-absolute microseconds. `backend` is the engine tag that actually served the request. `word_timing` is the provenance of the per-word times: `exact` (from an engine's token offsets) or `interpolated_from_cue` (approximated by splitting an SRT cue span across its words). Pipe the `srt` field straight into `apply_subtitles` (the cues self-position into a new caption track via their internal timestamps — `apply_subtitles` takes no start/end); use `segments`/`words` for word-level editing. Optional `t_start_us`/`t_end_us` narrow the transcription window inside the layer's time range; both default to the layer endpoints. Optional `backend` (`\"openai\"` | `\"whisper_cpp\"` | `\"funasr\"`) REQUIRES that engine: if it is not available the call errors naming the missing piece (key / binary / model) instead of substituting another engine, so an explicit local choice never falls back to a cloud upload; an unknown value is rejected. When omitted, selection is the user's preferred engine then availability. Optional `word_timestamps` (default true) requests exact per-word times when the chosen backend can emit them (whisper.cpp `-ojf`; FunASR always); pass false to force SRT-style interpolated output. OpenAI Whisper is SRT-only and ignores it. VideoClip layers with speed != 1.0 are rejected — split off a speed-1 segment first. Errors with structured messages if no transcription backend is configured (API key or local engine), the audio slice exceeds the provider cap (~13 min for Whisper at 25 MB), or the provider rate-limits / rejects auth.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "backend": {
            "default": null,
            "description": "Optional STRICT backend override: `\"openai\"` | `\"whisper_cpp\"` |\n`\"funasr\"`. When set, that engine serves the request or the call errors\nnaming its exact gap (missing key / binary / model) — it never\nsubstitutes another engine, so an explicit local choice can never leak\naudio to a cloud provider. When omitted, the resolver walks the user's\npreference then availability. An unknown value is rejected.",
            "type": [
              "string",
              "null"
            ]
          },
          "language": {
            "default": null,
            "description": "Optional ISO-639-1 language hint (`\"en\"`, `\"zh\"`). Auto-detect when omitted.",
            "type": [
              "string",
              "null"
            ]
          },
          "layer_id": {
            "description": "Target VideoClip or Audio layer id.",
            "type": "string"
          },
          "t_end_us": {
            "default": null,
            "description": "Optional transcription window end in timeline microseconds.\nDefaults to the layer's `t_end_us`. Must lie within the layer.",
            "format": "int64",
            "type": [
              "integer",
              "null"
            ]
          },
          "t_start_us": {
            "default": null,
            "description": "Optional transcription window start in timeline microseconds.\nDefaults to the layer's `t_start_us`. Must lie within the layer.",
            "format": "int64",
            "type": [
              "integer",
              "null"
            ]
          },
          "word_timestamps": {
            "default": null,
            "description": "Request exact per-word timestamps when the chosen backend can emit them\n(whisper.cpp → `-ojf`). Defaults to `true` — the backend's best\nprecision, at no extra engine cost; pass `false` to force the SRT-style\n(interpolated) output instead. OpenAI Whisper is SRT-only and ignores\nit either way; check `word_timing` in the result for what you got.",
            "type": [
              "boolean",
              "null"
            ]
          }
        },
        "required": [
          "layer_id"
        ],
        "title": "TranscribeClipArgs",
        "type": "object"
      }
    },
    {
      "name": "synthesize_speech",
      "description": "Synthesize speech via the configured cloud TTS provider (OpenAI tts-1 today) and attach the result as an Audio layer. The MP3 is content-addressed in cache by `(model, voice, speed, text)`, so a repeat call with the same args reuses the cached file without burning another API request. Args: `text` (≤4096 chars for tts-1), `voice` (one of alloy/echo/fable/onyx/nova/shimmer), optional `speed` (0.25..4.0; default = provider default ≈1.0), optional `target_track_id` (defaults to first existing Audio track or a new 'Voiceover' track), optional `t_start_us` (defaults to the composition's current duration so the voiceover appends at the end). Returns `{ layer_id, media_id, t_start_us, t_end_us, cached }`.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "speed": {
            "default": null,
            "description": "0.25..4.0 for tts-1. Omit to use the provider default (~1.0).",
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          },
          "t_start_us": {
            "default": null,
            "description": "Optional timeline start in microseconds. Defaults to the composition's\ncurrent `duration_us` so the voiceover appends at the end.",
            "format": "int64",
            "type": [
              "integer",
              "null"
            ]
          },
          "target_track_id": {
            "default": null,
            "description": "Optional Audio track id. If omitted, lands on the first existing Audio\ntrack or auto-creates one labeled \"Voiceover\".",
            "type": [
              "string",
              "null"
            ]
          },
          "text": {
            "description": "Text to synthesize. tts-1 caps at 4096 characters.",
            "type": "string"
          },
          "voice": {
            "description": "Voice identifier. tts-1 accepts: alloy, echo, fable, onyx, nova, shimmer.",
            "type": "string"
          }
        },
        "required": [
          "text",
          "voice"
        ],
        "title": "SynthesizeSpeechArgs",
        "type": "object"
      }
    },
    {
      "name": "describe_clip",
      "description": "Describe a VideoClip layer's visual content as timestamped, open-vocabulary segments using a video-understanding model (local Qwen3-VL / MiniCPM-V via llama-mtmd-cli, or any OpenAI-compatible endpoint). Samples frames from the layer's source at `fps` (default 1.0), runs the model once over the whole window, and returns `{ backend, model, segments: [{ t_start_us, t_end_us, text, tags: [ ... ] }] }`. All timestamps are SOURCE-ABSOLUTE microseconds; `backend`/`model` name the engine that actually served the request. `text` is a free-text description of the span; `tags` are short visual keywords (subjects, setting, camera motion, shot type) the agent can filter on. Results are cached per source range — a later call over an already-described window returns instantly with no model spawn. Optional `t_start_us`/`t_end_us` narrow the window inside the layer's time range (both default to the layer endpoints). Optional `fps` sets the sampling rate; `focus` (`\"general\"` | `\"shot-type\"`) selects the prompt template that populates `tags`. Optional `backend` (`\"qwen3_vl\"` | `\"minicpm_v\"` | `\"byo_endpoint\"`) REQUIRES that engine: if it is not available the call errors naming the missing piece (binary / model / endpoint) instead of substituting another engine, so an explicit local choice never uploads frames anywhere; an unknown value is rejected. When omitted, selection is the user's preferred engine then availability (local-first). VideoClip layers with speed != 1.0 are rejected — split off a speed-1 segment first. Errors with an actionable message when no video-understanding backend is configured.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "backend": {
            "default": null,
            "description": "Optional STRICT backend override: `\"qwen3_vl\"` | `\"minicpm_v\"` |\n`\"byo_endpoint\"`. When set, that engine serves the request or the call\nerrors naming its exact gap — it never substitutes another engine, so an\nexplicit local choice can never leak frames over the network.\nWhen omitted, selection is preference then availability. Unknown → rejected.",
            "type": [
              "string",
              "null"
            ]
          },
          "focus": {
            "default": null,
            "description": "Prompt focus: `\"general\"` (default) or `\"shot-type\"` (biases `tags`\ntoward shot type / camera).",
            "type": [
              "string",
              "null"
            ]
          },
          "fps": {
            "default": null,
            "description": "Frames sampled per second across the window (default 1.0). Higher = finer\ntemporal detail at more cost; capped so the frame set fits the model's\ncontext.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "layer_id": {
            "description": "Target VideoClip layer id.",
            "type": "string"
          },
          "t_end_us": {
            "default": null,
            "description": "Optional window end in timeline microseconds. Defaults to the layer's\n`t_end_us`. Must lie within the layer.",
            "format": "int64",
            "type": [
              "integer",
              "null"
            ]
          },
          "t_start_us": {
            "default": null,
            "description": "Optional window start in timeline microseconds. Defaults to the layer's\n`t_start_us`. Must lie within the layer.",
            "format": "int64",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "layer_id"
        ],
        "title": "DescribeClipArgs",
        "type": "object"
      }
    }
  ],
  "resources": [
    {
      "uri": "project://current",
      "name": "Current project",
      "description": "The full open WeftCut project as JSON. Re-fetch after change events.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://composition",
      "name": "Composition",
      "description": "Canvas size, fps, sample rate, color space, background.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://media",
      "name": "Media pool",
      "description": "All imported media items keyed by id.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://tracks",
      "name": "Tracks",
      "description": "Tracks with layer envelopes. Read project://layers/{id} for full layer detail.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://markers",
      "name": "Markers",
      "description": "Timeline markers, sorted by t_us.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://compositions",
      "name": "Compositions",
      "description": "Every composition — the root and each Group — with id, label, duration_us and ref_count (how many CompositionRef layers place it). project://tracks?composition=<id> and project://markers?composition=<id> read one of them; unscoped they read the root.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://history",
      "name": "History",
      "description": "Recent operations and named checkpoints (no snapshots). `ops` is a window: `window_start` is the absolute stack index of `ops[0]`, and `cursor` is absolute too — never an offset into `ops`; absolute indices are the only ones `jump_to` accepts. `evicted` > 0 means the oldest entries were dropped for good.",
      "mimeType": "application/json"
    },
    {
      "uri": "project://compiled",
      "name": "Audio mix plan",
      "description": "Compiled export-audio mix plan (layer placement on the 48 kHz frame grid + envelope summaries) — for agents that want structural reasoning about what export will mix.",
      "mimeType": "application/json"
    },
    {
      "uri": "composition://meter",
      "name": "Audio meter",
      "description": "Latest preview master-bus level reading (rms/peak dBFS). `live: false` when nothing has played in the last 2 seconds.",
      "mimeType": "application/json"
    }
  ],
  "prompts": [
    {
      "name": "cut-silences",
      "description": "Find silent regions in a clip and tighten it up by splitting + deleting the dead air.",
      "arguments": [
        {
          "name": "layer_id",
          "description": "Target VideoClip or Audio layer id.",
          "required": true
        },
        {
          "name": "threshold_amp",
          "description": "Peak amplitude threshold in [0.0, 1.0]. Default 0.02 (≈ -34 dBFS).",
          "required": false
        },
        {
          "name": "min_silence_us",
          "description": "Minimum silence duration to cut, in microseconds. Default 500000 (0.5s).",
          "required": false
        }
      ]
    },
    {
      "name": "auto-caption",
      "description": "Transcribe a video or audio layer with cloud Whisper, then apply the SRT as subtitles.",
      "arguments": [
        {
          "name": "layer_id",
          "description": "Target VideoClip or Audio layer id.",
          "required": true
        },
        {
          "name": "language",
          "description": "Optional ISO-639-1 language hint (en, zh, etc.). Auto-detect when omitted.",
          "required": false
        }
      ]
    },
    {
      "name": "voiceover",
      "description": "Generate cloud TTS for a script and attach it as an Audio layer.",
      "arguments": [
        {
          "name": "script",
          "description": "Text to speak. tts-1 caps a single call at 4096 chars; for longer scripts split into paragraphs.",
          "required": true
        },
        {
          "name": "voice",
          "description": "OpenAI voice: alloy, echo, fable, onyx, nova, or shimmer.",
          "required": false
        },
        {
          "name": "speed",
          "description": "Optional speech speed in [0.25, 4.0]. Omit for the provider default.",
          "required": false
        },
        {
          "name": "target_track_id",
          "description": "Optional Audio track id. Defaults to the first existing Audio track or a new 'Voiceover' track.",
          "required": false
        }
      ]
    }
  ]
}
